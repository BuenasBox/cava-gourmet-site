# Archivos fuente de cámara — no rastrear en el repo web
Assets/images/Nazareth/_Archivo/
Assets/images/Erick/_Archivo/
Assets/images/Nazareth y Erick/_Archivo/

# Staging local de logos optimizados (entrega de trabajo/reserva) —
# los archivos finales se copian a sus rutas reales en L-01/L-02/L-03
Assets/logo-optimizado/

# Formatos de cámara
*.ARW
*.arw
*.HEIF
*.heif
*.CR2
*.cr2
*.NEF
*.nef
*.DNG
*.dng

# Sistema operativo
.DS_Store
Thumbs.db

# Security audit output - never commit generated reports, logs, tokens, cookies, or credentials.
/security-reports/

# Variables de entorno — nunca subir al repo
.env
.env.local
.env.*.local

# Vercel — configuración local generada automáticamente
.vercel/

# Assets de marca aprobados pero NO integrados aún (sin sección editorial asignada).
# Ignorados a propósito para que no aparezcan como pendientes en Source Control
# y no se suban por accidente. Quitar estas líneas cuando se apruebe su integración.
cava-mark-light.webm
cava-mark-light.mp4
cava-mark-light-poster.jpg
cava-mark-hero.avif
cava-mark-hero.webp
